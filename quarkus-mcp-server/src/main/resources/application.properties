# Quarkus MCP Server config
quarkus.application.name=Weather MCP (Quarkus)
quarkus.mcp.server.sse.root-path=/mcp
# Bind to all interfaces for Azure Functions compatibility
quarkus.http.host=0.0.0.0
quarkus.http.port=${FUNCTIONS_CUSTOMHANDLER_PORT:8080}
# Enable logging of MCP JSON for troubleshooting
# quarkus.mcp.server.traffic-logging.enabled=true
# quarkus.mcp.server.traffic-logging.text-limit=500